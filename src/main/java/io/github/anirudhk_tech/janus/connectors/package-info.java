/**
 * Connector interfaces and implementations for external data sources (e.g. Postgres, HTTP APIs).
 * <p>
 * Connectors should normalize source-specific responses into a common result model.
 */
package io.github.anirudhk_tech.janus.connectors;


